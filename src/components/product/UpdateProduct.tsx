import React from "react";

function UpdateProduct() {
  /**
   * @todo
   * 1. you can get current product id from Product Context
   * 2. add getProductDetails action to get specific product details
   * 3. dispatch it to your currentFormData
   * 4. on submit follow the add product to handle update
   */
  return <h1>UpdateProduct</h1>;
}

export default UpdateProduct;
